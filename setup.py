from setuptools import find_packages, setup

setup(
    name="src",
    version="0.0.1",
    description="package for dl research template",
    packages=find_packages(),
    python_requires=">=3.6",
)